language: c
compiler:
  - gcc
  - clang
notifications:
  recipients:
    - kenhys@gmail.com
branches:
  only:
    - master
    - develop
before_script:
  - git submodule update --init --recursive
  - ./autogen.sh

